apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: public-organisations-api
  labels:
    app: public-organisations-api
    visualize: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: public-organisations-api
  template:
    metadata:
      labels:
        app: public-organisations-api
        visualize: "true"
    spec:
      containers:
      - name: public-organisations-api
        image: coco/public-organisations-api:v0.1.6
        imagePullPolicy: Always
        env:
        - name: NEO_URL
          value: "http://neo4j-red:7474/db/data/"
        - name: CACHE_DURATION
          value: 24h
        - name: GRAPHITE_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: global-config
              key: graphite.address
        - name: GRAPHITE_PREFIX
          value: coco.services.k8s.public-organisations-api
        resources:
          limits:
            memory: 256Mi
        ports:
        - containerPort: 8080
        livenessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 5
        readinessProbe:
          httpGet:
            path: /__gtg
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 30
---

kind: Service
apiVersion: v1
metadata:
  name: public-organisations-api
  labels:
    app: public-organisations-api
    visualize: "true"
    hasHealthcheck: "true"
spec:
  ports:
    - name: "app"
      port: 8080
      targetPort: 8080
  selector:
    app: public-organisations-api

