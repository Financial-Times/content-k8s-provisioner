apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: v1-annotations-rw-red
  labels:
    app: v1-annotations-rw-red
    visualize: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: v1-annotations-rw-red
  template:
    metadata:
      labels:
        app: v1-annotations-rw-red
        visualize: "true"
    spec:
      containers:
      - name: v1-annotations-rw-red
        image: coco/annotations-rw-neo4j:v0.1.8
        imagePullPolicy: Always
        resources:
          limits:
            memory: 256Mi
        livenessProbe:
          initialDelaySeconds: 5
          tcpSocket:
            port: 8080
        readinessProbe:
          httpGet:
            path: /__gtg
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 30
        env:
        - name: PLATFORM_VERSION
          value: "v1"
        - name: NEO_URL
          value: "http://neo4j-red:7474/db/data/"
        - name: BATCH_SIZE
          value: "50"
        - name: GRAPHITE_ADDRESS
          value: "graphite.ft.com:2003"
        - name: GRAPHITE_PREFIX
          value: "coco.services.k8s-test.v1-annotations-rw-neo4j-red"
        - name: LOG_METRICS
          value: "false"
        - name: APP_PORT
          value: "8080"
        ports:
        - containerPort: 8080

---

kind: Service
apiVersion: v1
metadata:
  name: v1-annotations-rw-red
  labels:
    app: v1-annotations-rw-red
    visualize: "true"
    hasHealthcheck: "true"
    healthcheckSeverity: "critical"
spec:
  ports:
    - port: 8080
      name: "8080"
      targetPort: 8080
  selector:
    app: v1-annotations-rw-red