apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: public-brands-api
  labels:
    app: public-brands-api
    visualize: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: public-brands-api
  template:
    metadata:
      labels:
        app: public-brands-api
        visualize: "true"
    spec:
      containers:
      - name: public-brands-api
        image: coco/public-brands-api:v0.0.9
        imagePullPolicy: Always
        env:
        - name: NEO_URL
          valueFrom:
            configMapKeyRef:
              name: global-config
              key: neo4j.read.only.url  
        - name: CACHE_DURATION
          value: 10m
        - name: GRAPHITE_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: global-config
              key: graphite.address
        - name: GRAPHITE_PREFIX
          value: coco.services.k8s.public-brands-api
        resources:
          limits:
            memory: 256Mi
        ports:
        - containerPort: 8080
        livenessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 5
        readinessProbe:
          httpGet:
            path: /__gtg
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 30
---

kind: Service
apiVersion: v1
metadata:
  name: public-brands-api
  labels:
    app: public-brands-api
    visualize: "true"
    hasHealthcheck: "true"
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: public-brands-api

