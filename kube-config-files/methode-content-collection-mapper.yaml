apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: methode-content-collection-mapper
  labels:
    app: methode-content-collection-mapper
    visualize: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: methode-content-collection-mapper
  template:
    metadata:
      labels:
        app: methode-content-collection-mapper
        visualize: "true"
    spec:
      containers:
      - name: methode-content-collection-mapper
        image: coco/methode-content-collection-mapper:remove-vulcan-routing
        imagePullPolicy: Always
        env:
        - name: JAVA_OPTS
          value: "-Xms128m -Xmx128m -XX:+UseG1GC -server"
        - name: KAFKA_PROXY_URL
          valueFrom:
            configMapKeyRef:
              name: global-config
              key: kafka.proxy.url
        ports:
        - containerPort: 8080
        - containerPort: 8081
        livenessProbe:
          initialDelaySeconds: 5
          tcpSocket:
            port: 8080
        readinessProbe:
          httpGet:
            path: /__gtg
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 30

---

kind: Service
apiVersion: v1
metadata:
  name: methode-content-collection-mapper
  labels:
    app: methode-content-collection-mapper
    visualize: "true"
    hasHealthcheck: "true"
spec:
  ports:
    - port: 8080
      name: "app"
      targetPort: 8080
    - port: 8081
      name: "admin"
      targetPort: 8081
  selector:
    app: methode-content-collection-mapper
