apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name:  public-things-api
  labels:
    app:  public-things-api
    visualize: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: public-things-api
  template:
    metadata:
      labels:
        app:  public-things-api
        visualize: "true"
    spec:
      containers:
      - name:  public-things-api
        image:  coco/public-things-api:0.0.17
        imagePullPolicy: Always
        resources:
          limits:
            memory: 256Mi
        env:
        - name: APP_PORT
          value: "8080" 
        - name: GRAPHITE_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: global-config
              key: graphite.address
        - name: GRAPHITE_PREFIX
          value: "coco.services.k8s.public-things-api"
        - name: LOG_METRICS
          value: "false" 
        - name: CACHE_DURATION
          value: "30s" 
        - name: NEO_URL
          valueFrom:
            configMapKeyRef:
              name: global-config
              key: neo4j.url  
        ports:
        - containerPort:  8080
        livenessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 10
        readinessProbe:
          httpGet:
            path: "/__gtg"
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 30

---

kind: Service
apiVersion: v1
metadata:
  name: public-things-api
  labels:
    app: public-things-api
    visualize: "true"
    hasHealthcheck: "true"
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: public-things-api
