apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: special-reports-rw-neo4j-red
  labels:
    app: special-reports-rw-neo4j-red
    visualize: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: special-reports-rw-neo4j-red
  template:
    metadata:
      labels:
        app: special-reports-rw-neo4j-red
        visualize: "true"
    spec:
      containers:
      - name: special-reports-rw-neo4j-red
        image: coco/special-reports-rw-neo4j:v1.1.3
        imagePullPolicy: Always
        resources:
          limits:
            memory: 256Mi
        env:
        - name: GRAPHITE_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: global-config
              key: graphite.address
        - name: GRAPHITE_PREFIX
          value: "coco.services.k8s.special-reports-rw-neo4j-red"
        - name: BATCH_SIZE
          valueFrom:
            configMapKeyRef:
              name: global-config
              key: neo4j.statements.batch.size	
        - name: NEO_URL
          value: "http://neo4j-red:7474/db/data/" 
        ports:
        - containerPort: 8080
        livenessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 10
        readinessProbe:
          httpGet:
            path: "/__gtg"
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 30

---

kind: Service
apiVersion: v1
metadata:
  name: special-reports-rw-neo4j-red
  labels:
    app: special-reports-rw-neo4j-red
    visualize: "true"
    hasHealthcheck: "true"
spec:
  ports:
    - port: 8080
      name: "app"
      targetPort: 8080
  selector:
    app: special-reports-rw-neo4j-red
