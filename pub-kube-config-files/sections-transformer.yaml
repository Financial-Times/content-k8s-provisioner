apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: sections-transformer
  labels:
    app: sections-transformer
    visualize: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sections-transformer
  template:
    metadata:
      labels:
        app: sections-transformer
        visualize: "true"
    spec:
      containers:
      - name: sections-transformer
        image: coco/sections-transformer:v1.0.4
        resources:
            limits:
              memory: 256Mi
        env:
        - name: PORT
          value: "8080"
        - name: BASE_URL
          value: "http://sections-transformer:8080/transformers/sections/"
        - name: TME_BASE_URL
          valueFrom:
            configMapKeyRef:
              name: global-config
              key: tme.url
        - name: TME_USERNAME
          valueFrom:
            secretKeyRef:
              name: global-secrets
              key: tme.username
        - name: TME_PASSWORD
          valueFrom:
            secretKeyRef:
              name: global-secrets
              key: tme.password
        - name: TOKEN
          valueFrom:
            secretKeyRef:
              name: global-secrets
              key: tme.token
        ports:
        - containerPort: 8080
        livenessProbe:
          initialDelaySeconds: 5
          tcpSocket:
            port: 8080
        readinessProbe:
          httpGet:
            path: /__gtg
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 30

---

kind: Service
apiVersion: v1
metadata:
  name: sections-transformer
  labels:
    app: sections-transformer
    visualize: "true"
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: sections-transformer
