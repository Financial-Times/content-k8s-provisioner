kind: PersistentVolume
apiVersion: v1
metadata:
  name: v1-brands-transformer-pv
  labels:
    failure-domain.beta.kubernetes.io/region: eu-west-1
    failure-domain.beta.kubernetes.io/zone: eu-west-1c
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 1Gi
  awsElasticBlockStore:
    fsType: ext4
    volumeID: vol-07ed6a824b9239459
  persistentVolumeReclaimPolicy: Retain
  claimRef:
    name: v1-brands-transformer-pvc
    namespace: default

---

kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: v1-brands-transformer-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---

apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: v1-brands-transformer
  labels:
    app: v1-brands-transformer
    visualize: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: v1-brands-transformer
  template:
    metadata:
      labels:
        app: v1-brands-transformer
        visualize: "true"
    spec:
      containers:
      - name: v1-brands-transformer
        image: coco/v1-brands-transformer:v1.2.3
        resources:
            limits:
              memory: 1536Mi
        env:
        - name: PORT
          value: "8080"
        - name: BASE_URL
          value: "http://v1-brands-transformer:8080/transformers/brands/"
        - name: TME_BASE_URL
          valueFrom:
            configMapKeyRef:
              name: global-config
              key: tme.url
        - name: TME_USERNAME
          valueFrom:
            secretKeyRef:
              name: global-secrets
              key: tme.username
        - name: TME_PASSWORD
          valueFrom:
            secretKeyRef:
              name: global-secrets
              key: tme.password
        - name: TOKEN
          valueFrom:
            secretKeyRef:
              name: global-secrets
              key: tme.token
        - name: CACHE_FILE_NAME
          value: "/cache/v1-brands-transformer.db"
        - name: BERTHA_SOURCE_URL
          value: "http://bertha.ig.ft.com/republish/publish/gss/1wEdVRLtayZ6-XBfYM3vKAGaOV64cNJD3L8MlLM8-uFY/TestBrands"
        - name: GRAPHITE_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: global-config
              key: graphite.address
        - name: GRAPHITE_PREFIX
          value: "coco.services.k8s.v1-brands-transformer"
        - name: LOG_METRICS
          value: "false"
        volumeMounts:
        - name: v1-brands-transformer-cache
          mountPath: /cache
        ports:
        - containerPort: 8080
        livenessProbe:
          initialDelaySeconds: 5
          tcpSocket:
            port: 8080
        readinessProbe:
          httpGet:
            path: /__gtg
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 30
      volumes:
      - name: v1-brands-transformer-cache
        persistentVolumeClaim:
          claimName: v1-brands-transformer-pvc

---

kind: Service
apiVersion: v1
metadata:
  name: v1-brands-transformer
  labels:
    app: v1-brands-transformer
    visualize: "true"
    hasHealthcheck: "true"
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: v1-brands-transformer
